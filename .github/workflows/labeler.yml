name: pr-labeler

on:
  pull_request:
    types: [opened, synchronize, reopened]

jobs:
  triage:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/labeler@main
      with:
        repo-token: ${{ secrets.RIOT_CI_ACCESS_TOKEN }}
        sync-labels: true
