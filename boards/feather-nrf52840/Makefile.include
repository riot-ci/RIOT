# http://pid.codes/1209/7D00/
USB_CONFIG_VID ?= 0x1209
USB_CONFIG_PID ?= 0x7D00

CFLAGS += -DUSB_CONFIG_VID=$(USB_CONFIG_VID) -DUSB_CONFIG_PID=$(USB_CONFIG_PID)

include $(RIOTBOARD)/common/nrf52/Makefile.include
